pre-commit:
  commands:
    lint:
      run: pnpm exec eslint {staged_files}
      glob:
        - "*.ts"
        - "*.tsx"
        - "*.js"
        - "*.jsx"

pre-push:
  commands:
    lint:
      run: pnpm lint

    typecheck:
      run: pnpm typecheck

    build:
      run: pnpm build